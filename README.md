#Cross-Chain-Rebase-Token

1. Protocol that allows users to create a vault and in return receive rebase token that represents their underlyning balance
2. Rebase token where balance of token is dynamic to show the changing balance in time:
                    .Balance increases linearly with time
                    .Mint tokens to the users everytime they perform an action(Mint, burn, bridging)
3. Interest rate